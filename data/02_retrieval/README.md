# Retrieval Corpus

This directory stores searchable corpus artifacts built from the canonical PDF register.

Expected files:

- `corpus.jsonl`
- `corpus_manifest.json`

The corpus is a chunk-level index used by retrieval enhancement and does not replace the raw PDF library under `data/01_raw/pdfs/`.
